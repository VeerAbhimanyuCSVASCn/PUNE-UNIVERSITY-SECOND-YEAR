
#include"E:\assignments\secon year sem 2\DS\4 - EXPRESSION TREES\tree.cpp"


int main()
{
    system("cls");
    system("color f0");

    string a,b;
    cout<<"ENETR AN EXPRESSION IN INFIX FORMAT for CONVERSION INTO POSTFIX FORMAT \n\n";
    cin>>a;
    fflush(stdin);
    b=converter(a);
    tree_substance * BHa = 0;treegenerator(b);    cout<<"2";
    fflush(stdin);
    fflush(stdin);
    fflush(stdin);

return 0;
}
